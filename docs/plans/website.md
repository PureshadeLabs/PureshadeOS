# Website (web/)

`web/` at repo root contains the OROS project website (HTML + SVG logo).

It has no build relationship to the OS itself. It is a candidate for extraction to
its own repo or a GitHub Pages branch when the project has a stable public identity.

Until then it lives at `web/` to keep it out of the way but in version control.
